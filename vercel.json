{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["public/**", "memos/**", "memos-en/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
